# Агрегатор цен Deribit 
 
Deribit Data Collector & API
. Приложение для сбора курсов криптовалют (BTC, ETH) с биржи Deribit и предоставления доступа к ним через REST API.
🛠 Стек технологий
Python 3.12
FastAPI (API интерфейс)
PostgreSQL (Хранение данных)
SQLAlchemy + Alembic (ORM и миграции)
Celery + Redis (Периодические задачи)
Aiohttp (Асинхронный клиент для API Deribit)
Pydantic Settings (Управление конфигурацией)
Docker & Docker Compose (Контейнеризация)
🚀 Как запустить (Docker)
Это самый быстрый способ развернуть проект со всеми зависимостями.
Клонируйте репозиторий:
bash
git clone <your-repository-url>
cd Deribit_data


Подготовьте файлы окружения:
1. Подготовка переменных окружения
Для работы приложения в Docker необходимо создать файл .env.docker в корневой папке проекта. Вы можете скопировать настройки ниже:
Создайте файл .env.docker:
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432
REDIS_HOST=redis
ENV_FILE_PATH=.env.docker


Запустите контейнеры:
bash
docker compose up -d --build


Примените миграции базы данных:
bash
docker compose exec api alembic upgrade head


Приложение доступно по адресу:
Swagger UI: http://localhost:8000/docs
API: http://localhost:8000/api/v1/...
💻 Локальная разработка (без Docker)
Установите зависимости: pip install -r requirements.txt.
Убедитесь, что у вас запущены локальные PostgreSQL и Redis.
Настройте файл .env (укажите localhost в качестве хостов).
Запустите API: uvicorn main:app --reload.
Запустите Celery Worker: celery -A app_deribit.worker.celery_app worker --loglevel=info.
Запустите в другом терминале: celery -A app_deribit.worker.tasks beat --loglevel=info
📝 Design Decisions (Принятые решения)
1. Архитектура проекта
Проект разделен на логические модули:
api/: Эндпоинты FastAPI и логика обработки запросов.
db_orm/: Модели SQLAlchemy, репозитории и конфигурация БД.
worker/: Логика Celery (tasks, scheduler) и клиент для биржи.
config/: Централизованное управление настройками через Pydantic Settings.
2. Сбор данных (Celery + Aiohttp)
Добавь это в секцию «Design Decisions» (вместо старого пункта 2):
2. Сбор данных (Celery + Aiohttp)
Периодичность: Использование Celery Beat позволяет точно соблюдать интервал в 60 секунд, запуская задачу create_all_prices.
Универсальность: Список отслеживаемых валют вынесен в конфиг (ticker_tuple = ('btc_usd', 'eth_usd') в файле config_data_aiohttp.py). Это позволяет легко масштабировать систему для мониторинга новых пар.
Async Client: Для запросов к бирже используется aiohttp. Клиент формирует динамические URL вида: https://test.deribit.com/api/v2/public/get_index_price?index_name={ticker}.
Асинхронность: Несмотря на то, что Celery работает синхронно, внутри задач используется asyncio.run(), что позволяет задействовать все преимущества асинхронного клиента aiohttp для высокоэффективного сетевого взаимодействия.
3. База данных и миграции
Использована SQLAlchemy (Async) для поддержки асинхронности FastAPI. Alembic выбран для управления схемой БД, что позволяет безопасно обновлять структуру таблиц в разных средах (локально/продакшн).
4. Контейнеризация
Приложение полностью изолировано в Docker. Для обеспечения гибкости реализована динамическая подгрузка .env файлов, что позволяет одной и той же кодовой базе работать как внутри контейнера, так и напрямую на хосте.
5. Безопасность и валидация
Все входящие и исходящие данные проходят валидацию через схемы Pydantic. Чувствительные данные (пароли БД) вынесены в переменные окружения.
📡 API Методы
Все запросы принимают обязательный параметр ticker (например, BTC или ETH).
GET /api/prices/?ticker=BTC — Все сохраненные данные.
GET /api/prices/last?ticker=BTC — Последняя актуальная цена.
GET /api/prices/period?ticker=BTC&start_year=...&end_year=... — История цен за период.
6. Тестирование API (unittest):
Для проверки бизнес-логики реализованы интеграционные тесты (в папке tests/). Тесты покрывают основные сценарии использования API:
Получение данных по тикеру.
Валидация обязательных query-параметров.
Корректность фильтрации по датам.
Обработка ошибок при запросе несуществующих тикеров.
